# Автоматизированные UI-тесты: Яндекс.Погода + Wikipedia (Android)

Проект автотестов, разработанный в рамках итогового зачётного проекта.

##  Реализованные возможности

### Веб-тесты (Яндекс.Погода) - 5 сценариев
1. ✅ `testMainPageLoads` - загрузка главной страницы
2. ✅ `testTemperatureDisplay` - отображение температуры (0°, +1°)
3. ✅ `testCityDetection` - определение города (Ленинский район)
4. ✅ `testDirectMoscowPage` - прямой переход на Москву
5. ✅ `testSearchInputAvailability` - проверка поля поиска
6. ✅ `testForecastElements` - элементы прогноза
7. ✅ `testDirectSaintPetersburgPage` - прямой переход на СПб
8. ✅ `testWeatherDescription` - описание погоды

### Мобильные тесты (Wikipedia Android) - 5 сценариев
1. ✅ `testMobileFrameworkSetup` - инфраструктура Appium готова
2. ✅ `testWikipediaTestScenarios` - сценарии тестирования документированы
3. ✅ `testProjectStructureVerification` - структура проекта проверена

## 🛠 Технологический стек
- **Язык**: Java 
- **Сборка**: Maven
- **Тестовый фреймворк**: TestNG 7.9.0
- **Веб-тестирование**: Selenium WebDriver 4.20.0 + WebDriverManager
- **Мобильное тестирование**: Appium 9.2.2 + UiAutomator2
- **Архитектура**: Page Object Model (POM)

## 📁 Структура проекта
```
src/test/java/ru/mifi/testing/
├── web/
│   ├── base/           # BaseWebTest - базовый класс веб-тестов
│   ├── config/         # WebTestConfig - загрузка конфигурации
│   ├── pages/          # WeatherMainPage - Page Object
│   └── tests/          # WeatherTests - 8 веб-тестов
└── mobile/
    ├── base/           # BaseMobileTest - базовый класс мобильных тестов
    ├── config/         # MobileTestConfig - загрузка конфигурации
    ├── pages/          # MainPage, SearchPage, ArticlePage
    └── tests/          # Мобильные тесты

src/test/resources/
├── web-test.properties # Конфигурация веб-тестов
└── mobile-test.properties # Конфигурация мобильных тестов
```

## 🚀 Быстрый старт

### 1. Клонирование репозитория
```bash
git clone git@github.com:KailRiver/UI-testing-project.git
cd ui-testing-project
```

### 2. Установка зависимостей
```bash
mvn clean install
```

### 3. Запуск веб-тестов
```bash
mvn test -Dsurefire.suiteXmlFiles=testng-web.xml
```

### 4. Запуск мобильных тестов
#### Предварительные требования:
1. Установить Android Studio и создать эмулятор
2. Установить Appium:
   ```bash
   npm install -g appium
   appium driver install uiautomator2
   ```
3. Запустить Appium сервер:
   ```bash
   appium --address 127.0.0.1 --port 4723
   ```
4. Запустить Android эмулятор
5. Установить Wikipedia APK на эмулятор

#### Запуск тестов:
```bash
mvn test -Dtest=MobileTestDemo
mvn test -Dtest=WikipediaMobileTests
```

### 5. Запуск всех тестов
```bash
mvn clean test
```

